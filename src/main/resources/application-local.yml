spring:
  datasource:
    username: root
    password: bootcamp2024
    url: jdbc:mysql://localhost:3306/db-naissances
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
logging:
  level:
    tech.sam: trace
